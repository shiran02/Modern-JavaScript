

// nested Loops -------------------------------

for(let i = 0 ; i < 10 ; i++){
    for(let i = 0 ; i < 5 ; i++){
        console.log(`${i} * ${j} = ${i * J}`);
    }
}

// loops through an array ---------------------

const names = ['brad' , 'sam' , 'john','Tim'];


for(let i = 0 ; i < names.length ; i++){
    if(names[i] === 'John'){
        console.log(names[i] + 'is The Nest');
    }else{
        console.log(name[i]);
    }
}